package VectoresyMatrices;

public class EjercicioPaygroundVectoresMatrices {
     /*
    public static void main(String[] args) {

        String[] ciudades = {"Londres", "Madrid", "Nueva York", "Buenos Aires", "Asunción", "São Paulo",
                "Lima", "Santiago de Chile", "Lisboa", "Tokio"};
        int[][] temperaturas = {{-2, 33}, {-3, 32}, {-8, 27}, {4, 37}, {6, 42}, {5, 43}, {0, 39}, {-7, 26}, {-1, 31}, {-10, 35}};

   // otra forma de hacerlo   integer.MIN_VALUE  integer.MAX_VALUE (CREAR ESTA VARIABLE)
   //Y PONERLA EN EL LUGAR DEL CERO

        int nombre_ciudad_temp_max = 0;
        int nombre_ciudad_temp_min = 0;

        int temp_max_ciudad = temperaturas[0][0];
        int temp_min_ciudad = temperaturas[0][1];

        for (int i = 0; i < temperaturas.length; i++) {
            //System.out.println(ciudades[i]);
            for (int j = 0; j < temperaturas[i].length; j++) {
                //System.out.println(temperaturas[i][j] + " "); // para cuando lo imprime lo hace en la misma linea
                if (temperaturas[i][j] < temp_min_ciudad) {
                    temp_min_ciudad  = temperaturas[i][j];
                    nombre_ciudad_temp_min = i;
                } else if (temperaturas[i][j]>temp_max_ciudad) {
                    temp_max_ciudad  = temperaturas[i][j];
                    nombre_ciudad_temp_max = i;

                }

            }
        }
        System.out.println("El valor mínimo es: " + temp_min_ciudad + "°C. Registrada en la ciudad de: " +
                ciudades[nombre_ciudad_temp_min] + ".");
        System.out.println("El valor maximo es: " + temp_max_ciudad + "°C. Registrada en la ciudad de: " +
                ciudades[nombre_ciudad_temp_max] + ".");

    }

      */
}


