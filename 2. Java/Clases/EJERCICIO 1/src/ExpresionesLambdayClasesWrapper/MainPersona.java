package ExpresionesLambdayClasesWrapper;

import java.util.ArrayList;
import java.util.List;

public class MainPersona {
    //ExpresionesLambday
/*
    public static void main(String[] args) {
        List<Persona> personas = new ArrayList<>();
        personas.add(new Persona("valentina", 34343));
        personas.add(new Persona("valentina2", 34341));
        personas.add(new Persona("valentina3", 453435));
        //Quiero las personas con dni mayor a 40000
        List<Persona> personas1 = new ArrayList<>();
        for (Persona persona : personas) {
            if (persona.getDni() > 40000) {
                personas1.add(persona);
            }
            System.out.println(persona);
        }
        for (Persona persona : personas1) {
            System.out.println(persona + "con dni mayor a 40000");
        }
        //personas.forEach(persona -> System.out.println(persona));
        //filtra por dni mayor
        personas.stream().filter(persona -> persona.getDni() > 40000).forEach(persona -> System.out.println(persona));

        //ordena de menor a mayor por dni
        personas.stream().sorted((p, q) -> p.getDni() >= q.getDni() ? 1 : -1).forEach(persona -> System.out.println(persona));

        //ordena de mayor a menor por dni
        personas.stream().sorted((p, q) -> p.getDni() >= q.getDni() ? -1 : 1).forEach(persona -> System.out.println(persona));

        //para hacer conversiones pasa lista de personas a lista dni
        personas.stream().mapToInt(p-> p.getDni()).forEach(e -> System.out.println(e));
        //suma de los dni
        System.out.println(personas.stream().mapToInt(p-> p.getDni()).sum());

    }

 */
}
