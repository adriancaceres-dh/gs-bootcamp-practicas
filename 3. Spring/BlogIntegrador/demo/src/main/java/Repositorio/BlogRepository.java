package Repositorio;

import model.EntradaBlog;
import org.springframework.stereotype.Repository;

import java.util.List;

@Repository
public class BlogRepository {
    private String data;

    public BlogRepository(){

        private List<EntradaBlog>
        data = "Hello world";

        //crear el generador de ID
           }

           public String getData(){
        return data;
           }

           public int save (EntradaBlog){
                blog.setid(autoIncrement);
                blogs.add(blog);
                autoIncrement++;//es interno

               int blogIdSaved = BlogRepository.save(blog)
                return blog.getid // es igual pero es como lo que quiero dar a conocer
           }
}
